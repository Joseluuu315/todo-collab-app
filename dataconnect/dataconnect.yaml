specVersion: "v1"
serviceId: "todo-collab-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "todo-collab-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
